<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "プロフィール画面に映る", current_user %></span>
<span><%= pluralize(current_user.points.count, "point") %></span>